#include <bits/stdc++.h>
using namespace std;
constexpr int64_t inf = (int64_t)1e+18;
constexpr int mod = 1000000007;

// @author: ZhockDen

void runCase(int &testCase) {
    // cout << "#Case " << testCase << ": \n";

    // int n, m;
    // cin >> n >> m;
    // assert(n >= 1 && n <= 100);
    // assert(m >= 1 && m <= 100);
}

int main() {

    ios::sync_with_stdio(false);
    cin.tie(0);

    int tests = 1;
    // cin >> tests;
    // assert(testCase >= 1 && testCase <= 100);
    for (int i = 1; i <= tests; i++) runCase(i);

    return 0;
}